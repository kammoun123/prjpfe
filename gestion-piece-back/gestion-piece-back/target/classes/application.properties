spring.application.name=gestion-piece-back

# Server port
server.port=8883

# Database configuration (MySQL) - Local
spring.datasource.url=jdbc:mysql://localhost:3307/piece?createDatabaseIfNotExist=true&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=
spring.jpa.open-in-view=false

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Let Hibernate 7 auto-detect the dialect from the JDBC connection (do NOT specify manually)
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.properties.hibernate.format_sql=false

# Mail configuration (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kammounkhalil10@gmail.com
spring.mail.password=rfgu chju jzse gapv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.test-connection=false

# File Upload Configuration
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Actuator
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.security.enabled=false
management.metrics.tags.application=${spring.application.name}
